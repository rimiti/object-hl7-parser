import parser from '../lib/parser'
import chai from 'chai'
chai.should()

describe('ADT', () => {
  it(`A04`, (done) => {
    done()
  })

  it(`A08`, (done) => {
    done()
  })

  it(`A40`, (done) => {
    done()
  })
})
