import parser from '../lib/parser'
import chai from 'chai'
chai.should()

describe('ADT', () => {
  //register a patient
  it(`A04`, (done) => {
    done()
  })
  //update a patient
  it(`A08`, (done) => {
    done()
  })
  //Merge patient - patient identifier list
  it(`A40`, (done) => {
    done()
  })
})
